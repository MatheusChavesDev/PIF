/* Exemplo Prático de Leitura */
#include <stdio.h>
#include <stdlib.h>

int main() {
    int a, b, c;
    /* Lê três valores decimais */
    scanf("%d %d %d" , &a, &b, &c);
    /* Imprime os valores lidos */
    printf("a:%d b:%d c:%d\n" , a, b, c);

    system("PAUSE");
    return 0;
}