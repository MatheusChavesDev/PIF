#include <stdio.h>
#include <stdlib.h>; --> diretivas não terminam com ";"
int Main{} --> usou "}" em vez de "()"
( --> deveria ser "{"
  printf( Existem %d semanas no ano.,52);--> não tem aspas ""
  cout << endl;--> não sei o que seria isso
  system("PAUSE");
  return 0;
)--> deveria ser "}"
